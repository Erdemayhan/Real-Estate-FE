const user = {
  getUsers: () => `/users`,
};

export { user };

const properties = {
  getAllProperties: () => `/properties`,
};

export { properties };

const Types = {
  getPropertyTypes: () => `/property_types`,
}

export { Types };